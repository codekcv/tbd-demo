import { describe, expect, it } from "vitest";

// Placeholder so the pipeline has something to run before the flag service lands.
// Delete once lib/flags has real tests.
describe("toolchain", () => {
  it("runs unit tests", () => {
    expect(1 + 1).toBe(2);
  });
});
