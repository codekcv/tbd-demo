# Provision with `npx convex dev` once. It writes CONVEX_DEPLOYMENT into .env.local
# and prints the deployment URL to put here.
NEXT_PUBLIC_CONVEX_URL=

# Used by CI and by the Vercel build to deploy Convex functions alongside the frontend.
# Generate in the Convex dashboard under Settings, Deploy Keys.
CONVEX_DEPLOY_KEY=
