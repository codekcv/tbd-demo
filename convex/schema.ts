import { defineSchema } from "convex/server";

// Intentionally empty. The flag tables arrive with the feature-flag-service change;
// see openspec/changes/feature-flag-service/design.md.
export default defineSchema({});
